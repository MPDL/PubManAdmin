import { SelectedColourDirective } from './selected-colour.directive';

describe('SelectedColourDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectedColourDirective();
    expect(directive).toBeTruthy();
  });
});
